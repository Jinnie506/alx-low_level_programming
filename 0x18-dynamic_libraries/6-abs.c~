#include "holberton.h"


/**
 * _abs - function that gives the absolute value of a number
 * @n:  is the int that will use for the argument of the function
 * Return: 0
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
